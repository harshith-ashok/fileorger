# Fileorger

---

manage your files and folders and search them all automatically with AI

- Send request to `chat`

```bash
curl -X POST http://localhost:8122 \
  -H "Content-Type: application/json" \
  -d '{"message":"Explain in one sentence what the sun is"}'
```
